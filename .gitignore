node_modules
dist
test-results
playwright-report
.DS_Store
*.log

# Nothing here creates these today. They are listed because a public repo is the
# wrong place to discover that something did.
.env
.env.*
coverage
*.tsbuildinfo
.vite

# Review/working notes. Kept on disk, deliberately not tracked in this repo —
# note the sibling labs DO track their chat.md, so this is a local divergence.
chat.md
chad.md

# Throwaway Playwright drivers used while checking the page in a real browser.
.pw-*.mjs
